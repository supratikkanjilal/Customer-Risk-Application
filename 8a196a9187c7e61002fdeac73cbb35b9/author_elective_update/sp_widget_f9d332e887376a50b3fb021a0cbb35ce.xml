<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="DELETE">
        <category>custom</category>
        <client_script><![CDATA[/*function ($scope, $http, spUtil, $location, spAriaFocusManager) {
	$scope.$on('data_table.click', function(e, parms){
		var p = $scope.data.page_id; //No defaulting to 'form' page 
		if(p) {
			var s = {id: p, table: parms.table, sys_id: parms.sys_id, view: 'sp'};
			$location.search(s);
		}
		var oid = $location.search().id;
		var p = $scope.data.page_id || 'form';
		var s = {id: p, table: parms.table, sys_id: parms.sys_id, view: $scope.data.view};
		if (oid == p) {
			s.spa = 1;
			var t = $location.search();
			s = angular.extend(t, s);
			$rootScope.$broadcast('$sp.list.click', s);
		}

		var newURL = $location.search(s);
		spAriaFocusManager.navigateToLink(newURL.url());	
	});
}*/

function ($scope, $location, spAriaFocusManager) {
  "use strict";
 
  // OPTIONAL: open form when a row is clicked (if you wire it from the template or row action)
  $scope.openRecord = function (row) {
    var params = angular.extend({}, $location.search(), {
      table: $scope.data.table,
      sys_id: row.sys_id,
      view:  $scope.data.view || 'sp'
    });
    $location.search(params);
    spAriaFocusManager.navigateToLink($location.absUrl());
  };
}
 ]]></client_script>
        <controller_as>c</controller_as>
        <css>/* Borrower-360: colored badges for Risk Band */
.risk-badge{
  display:inline-block;
  padding: 2px 8px;
  border-radius: 12px;
  font-weight: 600;
  line-height: 1;
}
 
.risk-high   { background:#d9534f; color:#fff; }   /* red   */
.risk-medium { background:#f0ad4e; color:#111; }   /* yellow*/
.risk-low    { background:#5cb85c; color:#fff; }   /* green */
 </css>
        <data_table>sp_instance_table</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list>title,table,filter,maximum_entries,order_direction,glyph,field_list,color,order_by,sp_page,sp_widget.script,sp_widget.client_script,sp_widget.template,sp_widget.css</field_list>
        <has_preview>true</has_preview>
        <id>clone_data_table</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {  }]]></link>
        <name>Clone Data Table</name>
        <option_schema/>
        <public>false</public>
        <roles/>
        <script><![CDATA[/*(function(){
	//  "use strict"; - linter issues 
	// populate the 'data' object
	var sp_page = $sp.getValue('sp_page');
	var pageGR = new GlideRecord('sp_page');
	pageGR.get(sp_page);
	data.page_id = pageGR.id.getDisplayValue();
	$sp.getValues(data);
	if (data.field_list) {
		data.fields_array = data.field_list.split(',');	
	} else {
		data.field_list = $sp.getListColumns(data.table);
	}

	if (input) {
		data.p = input.p;
		data.o = input.o;
		data.d = input.d;
		data.q = input.q;
	}
	data.p = data.p || 1;
	data.o = data.o || $sp.getValue('order_by');
	data.d = data.d || $sp.getValue('order_direction');

	data.page_index = (data.p - 1);
	data.window_size = $sp.getValue('maximum_entries') || 10;
	data.window_start  = (data.page_index * data.window_size);
	data.window_end = (((data.page_index + 1) * data.window_size));
	data.filter = $sp.getValue("filter");
	var filterField = options.filter_field || 'itfm_sb_stmt_def';
	if(filterField) {
		var value = $sp.getParameter(filterField);
		
		if(value) {
			var query = filterField + '=' + value;
			if(data.filter)
				data.filter = data.filter + '^' + query;
			else 
				data.filter = query;
		}
	}
	gs.info('data.filter = ' + data.filter);
	
	var gr = new GlideRecordSecure(data.table);
	if (!gr.isValid()) {
		data.invalid_table = true;
		data.table_label = data.table;
		return;
	}
	data.table_label = gr.getLabel();
	
	var widgetParams = {
		table: data.table, 
		fields: data.field_list,
		o: data.o,
		d: data.d,
		filter: data.filter,
		window_size: data.window_size,
		view: 'sp',
		title: options.title,
		show_breadcrumbs: true,
		headerTitle: options.title,
		useInstanceTitle: true
	};

	gs.info('widgetParams = ' + JSON.stringify(widgetParams));
	data.dataTableWidget = $sp.getWidget('widget-data-table', widgetParams);
})();*/


(function () {
  'use strict';
 
  // --- Resolve inputs (table, fields, filter) ---
  // Prefer instance options; fall back to URL or sensible defaults
  data.table  = (input && input.table)  || options.table  || $sp.getParameter('table')  || 'x_cpszo_spoke_customer_profile';
  data.view   = options.view || $sp.getParameter('view') || 'sp';
 
  // Ensure fields include the risk band column (adjust the API name if needed)
  var fieldList = (options.fields || 'name,number,u_risk_band')
                    .replace(/\s/g, ''); // strip spaces
  data.fields = fieldList;
 
  // Accept sysparm_query / filter from options or URL
  data.filter = options.filter || $sp.getParameter('sysparm_query') || '';
 
  // If the table is missing, stop early so the HTML can show the warning
  data.invalid_table = !data.table;
  data.table_label = data.table;
 
  if (data.invalid_table)
    return;
 
  // --- Build params for the inner OOTB Data Table widget ---
  var widgetParams = {
    table:  data.table,
    fields: data.fields,
    view:   data.view,
    filter: data.filter,
 
    // ]]>👇<![CDATA[ This is the key: map field value → CSS class
    field_styles: {
      // use your actual field name here (u_risk_band is from your screenshots)
      u_risk_band: {
        "High":   { "class": "risk-badge risk-high" },
        "Medium": { "class": "risk-badge risk-medium" },
        "Low":    { "class": "risk-badge risk-low" }
      }
    }
  };
 
  // Render the standard Data Table with our styling options
  // (Identifier is "widget-data-table" on most instances; if your clone used a different id,
  // keep that same id here.)
  data.dataTableWidget = $sp.getWidget('widget-data-table', widgetParams);
})();
 

]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>brijesh.banerjee</sys_created_by>
        <sys_created_on>2025-09-02 09:08:08</sys_created_on>
        <sys_id>f9d332e887376a50b3fb021a0cbb35ce</sys_id>
        <sys_mod_count>4</sys_mod_count>
        <sys_name>Clone Data Table</sys_name>
        <sys_package display_value="Spoke" source="x_cpszo_spoke">8a196a9187c7e61002fdeac73cbb35b9</sys_package>
        <sys_policy/>
        <sys_scope display_value="Spoke">8a196a9187c7e61002fdeac73cbb35b9</sys_scope>
        <sys_update_name>sp_widget_f9d332e887376a50b3fb021a0cbb35ce</sys_update_name>
        <sys_updated_by>brijesh.banerjee</sys_updated_by>
        <sys_updated_on>2025-09-02 10:27:14</sys_updated_on>
        <template><![CDATA[<div>
  <div class="alert alert-danger" ng-if="data.invalid_table">
    ${Table not defined} '{{data.table_label}}'
  </div>
  <sp-widget ng-if="data.dataTableWidget" widget="data.dataTableWidget"></sp-widget>
</div>]]></template>
    </sp_widget>
    <sys_update_version action="INSERT_OR_UPDATE">
        <action>DELETE</action>
        <application display_value="Spoke">8a196a9187c7e61002fdeac73cbb35b9</application>
        <file_path/>
        <instance_id>2baf7f71db9a0d1007072f17d496195c</instance_id>
        <instance_name>capgeminipolskaspzoodemo11</instance_name>
        <name>sp_widget_f9d332e887376a50b3fb021a0cbb35ce</name>
        <payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update table="sp_widget"&gt;&lt;sp_widget action="INSERT_OR_UPDATE"&gt;&lt;category&gt;custom&lt;/category&gt;&lt;client_script&gt;&lt;![CDATA[/*function ($scope, $http, spUtil, $location, spAriaFocusManager) {
	$scope.$on('data_table.click', function(e, parms){
		var p = $scope.data.page_id; //No defaulting to 'form' page 
		if(p) {
			var s = {id: p, table: parms.table, sys_id: parms.sys_id, view: 'sp'};
			$location.search(s);
		}
		var oid = $location.search().id;
		var p = $scope.data.page_id || 'form';
		var s = {id: p, table: parms.table, sys_id: parms.sys_id, view: $scope.data.view};
		if (oid == p) {
			s.spa = 1;
			var t = $location.search();
			s = angular.extend(t, s);
			$rootScope.$broadcast('$sp.list.click', s);
		}

		var newURL = $location.search(s);
		spAriaFocusManager.navigateToLink(newURL.url());	
	});
}*/

function ($scope, $location, spAriaFocusManager) {
  "use strict";
 
  // OPTIONAL: open form when a row is clicked (if you wire it from the template or row action)
  $scope.openRecord = function (row) {
    var params = angular.extend({}, $location.search(), {
      table: $scope.data.table,
      sys_id: row.sys_id,
      view:  $scope.data.view || 'sp'
    });
    $location.search(params);
    spAriaFocusManager.navigateToLink($location.absUrl());
  };
}
 ]]&gt;&lt;/client_script&gt;&lt;controller_as&gt;c&lt;/controller_as&gt;&lt;css&gt;/* Borrower-360: colored badges for Risk Band */
.risk-badge{
  display:inline-block;
  padding: 2px 8px;
  border-radius: 12px;
  font-weight: 600;
  line-height: 1;
}
 
.risk-high   { background:#d9534f; color:#fff; }   /* red   */
.risk-medium { background:#f0ad4e; color:#111; }   /* yellow*/
.risk-low    { background:#5cb85c; color:#fff; }   /* green */
 &lt;/css&gt;&lt;data_table&gt;sp_instance_table&lt;/data_table&gt;&lt;demo_data/&gt;&lt;description/&gt;&lt;docs/&gt;&lt;field_list&gt;title,table,filter,maximum_entries,order_direction,glyph,field_list,color,order_by,sp_page,sp_widget.script,sp_widget.client_script,sp_widget.template,sp_widget.css&lt;/field_list&gt;&lt;has_preview&gt;true&lt;/has_preview&gt;&lt;id&gt;clone_data_table&lt;/id&gt;&lt;internal&gt;false&lt;/internal&gt;&lt;link&gt;&lt;![CDATA[function link(scope, element, attrs, controller) {  }]]&gt;&lt;/link&gt;&lt;name&gt;Clone Data Table&lt;/name&gt;&lt;option_schema/&gt;&lt;public&gt;false&lt;/public&gt;&lt;roles/&gt;&lt;script&gt;&lt;![CDATA[/*(function(){
	//  "use strict"; - linter issues 
	// populate the 'data' object
	var sp_page = $sp.getValue('sp_page');
	var pageGR = new GlideRecord('sp_page');
	pageGR.get(sp_page);
	data.page_id = pageGR.id.getDisplayValue();
	$sp.getValues(data);
	if (data.field_list) {
		data.fields_array = data.field_list.split(',');	
	} else {
		data.field_list = $sp.getListColumns(data.table);
	}

	if (input) {
		data.p = input.p;
		data.o = input.o;
		data.d = input.d;
		data.q = input.q;
	}
	data.p = data.p || 1;
	data.o = data.o || $sp.getValue('order_by');
	data.d = data.d || $sp.getValue('order_direction');

	data.page_index = (data.p - 1);
	data.window_size = $sp.getValue('maximum_entries') || 10;
	data.window_start  = (data.page_index * data.window_size);
	data.window_end = (((data.page_index + 1) * data.window_size));
	data.filter = $sp.getValue("filter");
	var filterField = options.filter_field || 'itfm_sb_stmt_def';
	if(filterField) {
		var value = $sp.getParameter(filterField);
		
		if(value) {
			var query = filterField + '=' + value;
			if(data.filter)
				data.filter = data.filter + '^' + query;
			else 
				data.filter = query;
		}
	}
	gs.info('data.filter = ' + data.filter);
	
	var gr = new GlideRecordSecure(data.table);
	if (!gr.isValid()) {
		data.invalid_table = true;
		data.table_label = data.table;
		return;
	}
	data.table_label = gr.getLabel();
	
	var widgetParams = {
		table: data.table, 
		fields: data.field_list,
		o: data.o,
		d: data.d,
		filter: data.filter,
		window_size: data.window_size,
		view: 'sp',
		title: options.title,
		show_breadcrumbs: true,
		headerTitle: options.title,
		useInstanceTitle: true
	};

	gs.info('widgetParams = ' + JSON.stringify(widgetParams));
	data.dataTableWidget = $sp.getWidget('widget-data-table', widgetParams);
})();*/


(function () {
  'use strict';
 
  // --- Resolve inputs (table, fields, filter) ---
  // Prefer instance options; fall back to URL or sensible defaults
  data.table  = (input &amp;&amp; input.table)  || options.table  || $sp.getParameter('table')  || 'x_cpszo_spoke_customer_profile';
  data.view   = options.view || $sp.getParameter('view') || 'sp';
 
  // Ensure fields include the risk band column (adjust the API name if needed)
  var fieldList = (options.fields || 'name,number,u_risk_band')
                    .replace(/\s/g, ''); // strip spaces
  data.fields = fieldList;
 
  // Accept sysparm_query / filter from options or URL
  data.filter = options.filter || $sp.getParameter('sysparm_query') || '';
 
  // If the table is missing, stop early so the HTML can show the warning
  data.invalid_table = !data.table;
  data.table_label = data.table;
 
  if (data.invalid_table)
    return;
 
  // --- Build params for the inner OOTB Data Table widget ---
  var widgetParams = {
    table:  data.table,
    fields: data.fields,
    view:   data.view,
    filter: data.filter,
 
    // ]]&gt;👇&lt;![CDATA[ This is the key: map field value → CSS class
    field_styles: {
      // use your actual field name here (u_risk_band is from your screenshots)
      u_risk_band: {
        "High":   { "class": "risk-badge risk-high" },
        "Medium": { "class": "risk-badge risk-medium" },
        "Low":    { "class": "risk-badge risk-low" }
      }
    }
  };
 
  // Render the standard Data Table with our styling options
  // (Identifier is "widget-data-table" on most instances; if your clone used a different id,
  // keep that same id here.)
  data.dataTableWidget = $sp.getWidget('widget-data-table', widgetParams);
})();
 

]]&gt;&lt;/script&gt;&lt;servicenow&gt;false&lt;/servicenow&gt;&lt;sys_class_name&gt;sp_widget&lt;/sys_class_name&gt;&lt;sys_created_by&gt;brijesh.banerjee&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2025-09-02 09:08:08&lt;/sys_created_on&gt;&lt;sys_id&gt;f9d332e887376a50b3fb021a0cbb35ce&lt;/sys_id&gt;&lt;sys_mod_count&gt;4&lt;/sys_mod_count&gt;&lt;sys_name&gt;Clone Data Table&lt;/sys_name&gt;&lt;sys_package display_value="Spoke" source="x_cpszo_spoke"&gt;8a196a9187c7e61002fdeac73cbb35b9&lt;/sys_package&gt;&lt;sys_policy/&gt;&lt;sys_scope display_value="Spoke"&gt;8a196a9187c7e61002fdeac73cbb35b9&lt;/sys_scope&gt;&lt;sys_update_name&gt;sp_widget_f9d332e887376a50b3fb021a0cbb35ce&lt;/sys_update_name&gt;&lt;sys_updated_by&gt;brijesh.banerjee&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2025-09-02 10:27:14&lt;/sys_updated_on&gt;&lt;template&gt;&lt;![CDATA[&lt;div&gt;
  &lt;div class="alert alert-danger" ng-if="data.invalid_table"&gt;
    ${Table not defined} '{{data.table_label}}'
  &lt;/div&gt;
  &lt;sp-widget ng-if="data.dataTableWidget" widget="data.dataTableWidget"&gt;&lt;/sp-widget&gt;
&lt;/div&gt;]]&gt;&lt;/template&gt;&lt;/sp_widget&gt;&lt;/record_update&gt;</payload>
        <payload_hash>-1219176259</payload_hash>
        <record_name>Clone Data Table</record_name>
        <reverted_from/>
        <source>9a0c392b878ba650b3fb021a0cbb3575</source>
        <source_table>sys_update_set</source_table>
        <state>previous</state>
        <sys_created_by>brijesh.banerjee</sys_created_by>
        <sys_created_on>2025-09-02 10:27:14</sys_created_on>
        <sys_id>d0f583e087f76a50b3fb021a0cbb35e6</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_recorded_at>19909f76dd60000001</sys_recorded_at>
        <sys_updated_by>brijesh.banerjee</sys_updated_by>
        <sys_updated_on>2025-09-02 10:27:14</sys_updated_on>
        <type>Widget</type>
        <update_guid>18f583e0c9f76a50ce6fd7a8f8ead2e5</update_guid>
        <update_guid_history>18f583e0c9f76a50ce6fd7a8f8ead2e5:-1219176259,17950ba0f7f76a5027b1923e97892b5c:539983897,ce55076073f76a501d04cdf1805163d5:1431164448,80757a6cf0376a5078d319490ec66bf9:798011705,bdd332e87f376a502420c9362c173fd0:1054476304</update_guid_history>
    </sys_update_version>
    <sys_metadata_delete action="INSERT_OR_UPDATE">
        <sys_audit_delete/>
        <sys_class_name>sys_metadata_delete</sys_class_name>
        <sys_created_by>brijesh.banerjee</sys_created_by>
        <sys_created_on>2025-09-04 04:13:35</sys_created_on>
        <sys_db_object display_value="" name="sp_widget">sp_widget</sys_db_object>
        <sys_id>38f22387d7344a009509840f23040000</sys_id>
        <sys_metadata>f9d332e887376a50b3fb021a0cbb35ce</sys_metadata>
        <sys_mod_count>0</sys_mod_count>
        <sys_name>Clone Data Table</sys_name>
        <sys_package display_value="Spoke" source="x_cpszo_spoke">8a196a9187c7e61002fdeac73cbb35b9</sys_package>
        <sys_parent/>
        <sys_policy/>
        <sys_scope display_value="Spoke">8a196a9187c7e61002fdeac73cbb35b9</sys_scope>
        <sys_scope_delete display_value="">244a12c00e714612af9745888ca7e024</sys_scope_delete>
        <sys_update_name>sp_widget_f9d332e887376a50b3fb021a0cbb35ce</sys_update_name>
        <sys_update_version display_value="sp_widget_f9d332e887376a50b3fb021a0cbb35ce">d0f583e087f76a50b3fb021a0cbb35e6</sys_update_version>
        <sys_updated_by>brijesh.banerjee</sys_updated_by>
        <sys_updated_on>2025-09-04 04:13:35</sys_updated_on>
    </sys_metadata_delete>
</record_update>
